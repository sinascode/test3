apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  aws.region: "ap-northeast-2"
  s3.bucket.name: "team4-memo-images"
  redis.host: "your-elasticache-endpoint.cache.amazonaws.com"
  redis.port: "6379"
  redis.ttl: "86400"
  app.upload.max.size: "10MB"
